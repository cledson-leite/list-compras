import { StyleSheet } from "react-native";

export const styles =  (colors: any) =>StyleSheet.create({
  title: {
    fontSize: 32,
    color: colors
  },
  subtitle: {
    fontSize: 20,
    color: colors
  },
  body: {
    fontSize: 16,
    color: colors
  },
});