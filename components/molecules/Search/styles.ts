import { StyleSheet } from 'react-native'

export const styles = (_colors: any) =>
  StyleSheet.create({
    container: {
      width: '100%',
      paddingHorizontal: 16,
      marginTop: 8,
      marginBottom: 4,
    },
  })

